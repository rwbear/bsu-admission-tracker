{
  "name": "admission-tracker",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Live Belarus university admission chance radar for GitHub Pages",
  "scripts": {
    "scrape": "node scripts/scrape/run.mjs",
    "test": "node --test tests/*.test.js",
    "fixtures": "node scripts/scrape/write-fixtures.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}

/**
 * Copy for «Как обновляются данные».
 * Shared so the sheet and any future surface stay in lockstep.
 */

export const UPDATES_TITLE = 'Как обновляются данные';

export const UPDATES_LEDE =
  'Мы не подключены к БГУ напрямую. Робот заходит на abit.bsu.by примерно раз в 5 минут, снимает таблицы и кладёт свежий JSON в репозиторий. Страница читает этот JSON и показывает его тебе.';

/** @type {readonly { term: string, def: string }[]} */
export const UPDATES_FACTS = Object.freeze([
  Object.freeze({
    term: 'Обновлено',
    def: 'Время последнего успешного снимка от робота — то, что ты видишь на странице сейчас.',
  }),
  Object.freeze({
    term: 'Ещё m:ss',
    def: 'Через сколько страница сама пойдёт проверять, не появился ли новый снимок. Обычный интервал — 3 минуты.',
  }),
  Object.freeze({
    term: 'Обновляю…',
    def: 'Прямо сейчас проверяем репозиторий. Если данные не изменились, страница ничего не перерисует.',
  }),
  Object.freeze({
    term: 'Ждём свежий сбор',
    def: 'Последний снимок старше обычного шага (6 минут). Пока роботу не удалось положить новый — опрашиваем чаще, раз в 30 секунд.',
  }),
  Object.freeze({
    term: 'Точка слева',
    def: 'Серая — ждём следующий опрос. Тёмная пульсирует — идёт проверка. Янтарная — ждём свежий сбор.',
  }),
]);

export const UPDATES_FOOT =
  'Мы всегда стараемся показать самый свежий снимок: сначала по прямой ссылке на коммит, потом через GitHub Pages, потом с ветки. Если один канал отстал — берём с другого. Ничего у тебя на устройстве не хранится, кроме введённого балла и выбранных фильтров.';

export const UPDATES_ARIA_LABELS = Object.freeze({
  /**
   * @param {string} ageText
   * @param {string} countdownText
   */
  idle: (ageText, countdownText) =>
    `Обновлено ${ageText}. Следующая проверка через ${countdownText}. Открыть, как это работает.`,
  /**
   * @param {string} ageText
   */
  fetching: (ageText) =>
    `Обновлено ${ageText}. Идёт проверка. Открыть, как это работает.`,
  /**
   * @param {string} ageText
   */
  chase: (ageText) =>
    `Обновлено ${ageText}. Ждём свежий сбор. Открыть, как это работает.`,
  loading: 'Загрузка данных. Открыть, как это работает.',
});
